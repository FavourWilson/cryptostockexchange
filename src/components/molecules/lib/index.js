"use client";
import {
  Dropdown as dropdown,
  Navbar as navbar,
  Avatar as avatar,
  Card as card,
} from "flowbite-react";
import Image from "next/image";
import { Scrollbar as scrollbar, Pagination as pagination } from "swiper";
import { Swiper as swiper, SwiperSlide as swiperslide } from "swiper/react";

import PerfectScrollbar from "react-perfect-scrollbar";

export const Dropdown = dropdown;
export const Navbar = navbar;
export const Avatar = avatar;
export const Card = card;
export const Img = Image;
export const Scrollbar = scrollbar;
export const Pagination = pagination;
export const Swiper = swiper;
export const SwiperSlide = swiperslide;
export const PS = PerfectScrollbar;
